# Knowledge Base

Structured reference material for supported forms, common field mappings, prompt templates, and translations.

## Suggested usage

- Store form-specific guidance under `government_forms/`.
- Store label-to-field mappings under `field_mappings/`.
- Store reusable prompt fragments under `prompts/`.
- Store multilingual phrases under `translations/`.
